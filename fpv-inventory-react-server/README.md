# FPV Inventory (React + Vite + Tailwind + FastAPI + Mongo)

## Frontend (this folder)
Create `.env` from example and set API base (defaults to http://localhost:8000):

```
cp .env.example .env
npm install
npm run dev
```

## Backend
Use the companion archive `fpv-inventory-backend.zip`.
